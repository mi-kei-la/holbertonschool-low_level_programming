#include <stdio.h>
#include "holberton.h"

/**
 * rev_string - reverse a string
 *
 * @s: parameter
 */

void rev_string(char *s)
{
	char start, end, temp;

	for (end = 0; s[end] != '\0'; end++)
	{
	}

	for (start = s[0]; 
}
